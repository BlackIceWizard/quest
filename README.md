# Telegram Quest

## Installation

#### Docker

Create `.env` and .env.local files:

```bash
$ make envfile
```

Build, run and install:

```bash
$ make
```

Login into container:

```bash
$ make ssh
```

Launch API documentation:

// In progress